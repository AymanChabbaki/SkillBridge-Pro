// PaymentFlowMock.tsx
// NOTE: The mock payments page was removed in favor of the real `Payments` page.
// This stub remains to avoid accidental import breakage in older branches.

const _stub = null as unknown as React.FC;

export default _stub;
